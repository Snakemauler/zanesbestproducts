<% if signed_in? %>
  <h4>Add a comment:</h4> <p>
  <%= form_for([@post, @post.feedbacks.build]) do |f| %> </p>
    <div class="field">
      <%= f.text_area :body, style: "resize:none", rows: "5", cols: "40" %>
    </div>
    <hr>
      <%= f.submit "Submit" %>
  <% end %>
<% end %>